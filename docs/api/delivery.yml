---
components:
  responses: {}
  schemas:
    DeliveryLog:
      additionalProperties: false
      description: A delivery job log entry for a notification channel.
      properties:
        attempted_at:
          format: date-time
          nullable: true
          type: string
          x-struct:
          x-validate:
        errors:
          items:
            nullable: true
            type: string
            x-struct:
            x-validate:
          type: array
          x-struct:
          x-validate:
        event:
          nullable: true
          type: string
          x-struct:
          x-validate:
        id:
          type: integer
          x-struct:
          x-validate:
        inserted_at:
          format: date-time
          type: string
          x-struct:
          x-validate:
        status:
          enum:
            - success
            - failed
          type: string
          x-struct:
          x-validate:
        worker:
          type: string
          x-struct:
          x-validate:
      required:
        - id
        - status
        - worker
        - inserted_at
      title: DeliveryLog
      type: object
      x-struct:
      x-validate:
    DeliveryLogList:
      additionalProperties: false
      description: A paginated list of delivery log entries for a notification channel.
      properties:
        data:
          items:
            additionalProperties: false
            description: A delivery job log entry for a notification channel.
            properties:
              attempted_at:
                format: date-time
                nullable: true
                type: string
                x-struct:
                x-validate:
              errors:
                items:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                type: array
                x-struct:
                x-validate:
              event:
                nullable: true
                type: string
                x-struct:
                x-validate:
              id:
                type: integer
                x-struct:
                x-validate:
              inserted_at:
                format: date-time
                type: string
                x-struct:
                x-validate:
              status:
                enum:
                  - success
                  - failed
                type: string
                x-struct:
                x-validate:
              worker:
                type: string
                x-struct:
                x-validate:
            required:
              - id
              - status
              - worker
              - inserted_at
            title: DeliveryLog
            type: object
            x-struct:
            x-validate:
          type: array
          x-struct:
          x-validate:
        meta:
          additionalProperties: false
          properties:
            page:
              type: integer
              x-struct:
              x-validate:
            page_size:
              type: integer
              x-struct:
              x-validate:
            total_pages:
              type: integer
              x-struct:
              x-validate:
          type: object
          x-struct:
          x-validate:
      title: DeliveryLogList
      type: object
      x-struct:
      x-validate:
    EmailChannel:
      additionalProperties: false
      description: Email-specific configuration.
      nullable: true
      properties:
        address:
          type: string
          x-struct:
          x-validate:
        anti_phishing_code:
          description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
          type: string
          x-struct:
          x-validate:
        settings:
          additionalProperties: true
          type: object
          x-struct:
          x-validate:
      required:
        - address
        - settings
        - anti_phishing_code
      title: EmailChannel
      type: object
      x-struct:
      x-validate:
    Error:
      properties:
        error:
          properties:
            code:
              type: string
              x-struct:
              x-validate:
            message:
              type: string
              x-struct:
              x-validate:
          type: object
          x-struct:
          x-validate:
      title: Error
      type: object
      x-struct:
      x-validate:
    NotificationChannel:
      additionalProperties: false
      description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
      properties:
        email_channel:
          additionalProperties: false
          description: Email-specific configuration.
          nullable: true
          properties:
            address:
              type: string
              x-struct:
              x-validate:
            anti_phishing_code:
              description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
              type: string
              x-struct:
              x-validate:
            settings:
              additionalProperties: true
              type: object
              x-struct:
              x-validate:
          required:
            - address
            - settings
            - anti_phishing_code
          title: EmailChannel
          type: object
          x-struct:
          x-validate:
        id:
          format: uuid
          type: string
          x-struct:
          x-validate:
        inserted_at:
          format: date-time
          type: string
          x-struct:
          x-validate:
        name:
          type: string
          x-struct:
          x-validate:
        type:
          enum:
            - email
            - webhook
          type: string
          x-struct:
          x-validate:
        updated_at:
          format: date-time
          type: string
          x-struct:
          x-validate:
        webhook_channel:
          additionalProperties: false
          description: Webhook-specific configuration.
          nullable: true
          properties:
            settings:
              additionalProperties: true
              type: object
              x-struct:
              x-validate:
            signing_token:
              description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
              type: string
              x-struct:
              x-validate:
            url:
              type: string
              x-struct:
              x-validate:
          required:
            - url
            - settings
            - signing_token
          title: WebhookChannel
          type: object
          x-struct:
          x-validate:
        workspace_id:
          format: uuid
          type: string
          x-struct:
          x-validate:
      required:
        - id
        - workspace_id
        - name
        - type
        - webhook_channel
        - email_channel
        - inserted_at
        - updated_at
      title: NotificationChannel
      type: object
      x-struct:
      x-validate:
    NotificationChannelCreateRequest:
      additionalProperties: false
      description: Parameters for creating a notification channel.
      properties:
        name:
          maxLength: 255
          minLength: 1
          type: string
          x-struct:
          x-validate:
        settings:
          additionalProperties: true
          nullable: true
          type: object
          x-struct:
          x-validate:
        target:
          maxLength: 2048
          minLength: 1
          type: string
          x-struct:
          x-validate:
        type:
          enum:
            - email
            - webhook
          type: string
          x-struct:
          x-validate:
      required:
        - name
        - type
        - target
      title: NotificationChannelCreateRequest
      type: object
      x-struct:
      x-validate:
    NotificationChannelList:
      description: Paginated list of notification channels.
      properties:
        data:
          items:
            additionalProperties: false
            description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
            properties:
              email_channel:
                additionalProperties: false
                description: Email-specific configuration.
                nullable: true
                properties:
                  address:
                    type: string
                    x-struct:
                    x-validate:
                  anti_phishing_code:
                    description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
                    type: string
                    x-struct:
                    x-validate:
                  settings:
                    additionalProperties: true
                    type: object
                    x-struct:
                    x-validate:
                required:
                  - address
                  - settings
                  - anti_phishing_code
                title: EmailChannel
                type: object
                x-struct:
                x-validate:
              id:
                format: uuid
                type: string
                x-struct:
                x-validate:
              inserted_at:
                format: date-time
                type: string
                x-struct:
                x-validate:
              name:
                type: string
                x-struct:
                x-validate:
              type:
                enum:
                  - email
                  - webhook
                type: string
                x-struct:
                x-validate:
              updated_at:
                format: date-time
                type: string
                x-struct:
                x-validate:
              webhook_channel:
                additionalProperties: false
                description: Webhook-specific configuration.
                nullable: true
                properties:
                  settings:
                    additionalProperties: true
                    type: object
                    x-struct:
                    x-validate:
                  signing_token:
                    description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
                    type: string
                    x-struct:
                    x-validate:
                  url:
                    type: string
                    x-struct:
                    x-validate:
                required:
                  - url
                  - settings
                  - signing_token
                title: WebhookChannel
                type: object
                x-struct:
                x-validate:
              workspace_id:
                format: uuid
                type: string
                x-struct:
                x-validate:
            required:
              - id
              - workspace_id
              - name
              - type
              - webhook_channel
              - email_channel
              - inserted_at
              - updated_at
            title: NotificationChannel
            type: object
            x-struct:
            x-validate:
          type: array
          x-struct:
          x-validate:
      required:
        - data
      title: NotificationChannelList
      type: object
      x-struct:
      x-validate:
    NotificationChannelResponse:
      description: Single notification channel response.
      properties:
        data:
          additionalProperties: false
          description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
          properties:
            email_channel:
              additionalProperties: false
              description: Email-specific configuration.
              nullable: true
              properties:
                address:
                  type: string
                  x-struct:
                  x-validate:
                anti_phishing_code:
                  description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
                  type: string
                  x-struct:
                  x-validate:
                settings:
                  additionalProperties: true
                  type: object
                  x-struct:
                  x-validate:
              required:
                - address
                - settings
                - anti_phishing_code
              title: EmailChannel
              type: object
              x-struct:
              x-validate:
            id:
              format: uuid
              type: string
              x-struct:
              x-validate:
            inserted_at:
              format: date-time
              type: string
              x-struct:
              x-validate:
            name:
              type: string
              x-struct:
              x-validate:
            type:
              enum:
                - email
                - webhook
              type: string
              x-struct:
              x-validate:
            updated_at:
              format: date-time
              type: string
              x-struct:
              x-validate:
            webhook_channel:
              additionalProperties: false
              description: Webhook-specific configuration.
              nullable: true
              properties:
                settings:
                  additionalProperties: true
                  type: object
                  x-struct:
                  x-validate:
                signing_token:
                  description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
                  type: string
                  x-struct:
                  x-validate:
                url:
                  type: string
                  x-struct:
                  x-validate:
              required:
                - url
                - settings
                - signing_token
              title: WebhookChannel
              type: object
              x-struct:
              x-validate:
            workspace_id:
              format: uuid
              type: string
              x-struct:
              x-validate:
          required:
            - id
            - workspace_id
            - name
            - type
            - webhook_channel
            - email_channel
            - inserted_at
            - updated_at
          title: NotificationChannel
          type: object
          x-struct:
          x-validate:
      required:
        - data
      title: NotificationChannelResponse
      type: object
      x-struct:
      x-validate:
    NotificationChannelUpdateRequest:
      additionalProperties: false
      description: Parameters for updating a notification channel.
      properties:
        name:
          maxLength: 255
          minLength: 1
          type: string
          x-struct:
          x-validate:
        settings:
          additionalProperties: true
          nullable: true
          type: object
          x-struct:
          x-validate:
        target:
          maxLength: 2048
          minLength: 1
          type: string
          x-struct:
          x-validate:
      title: NotificationChannelUpdateRequest
      type: object
      x-struct:
      x-validate:
    WebhookChannel:
      additionalProperties: false
      description: Webhook-specific configuration.
      nullable: true
      properties:
        settings:
          additionalProperties: true
          type: object
          x-struct:
          x-validate:
        signing_token:
          description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
          type: string
          x-struct:
          x-validate:
        url:
          type: string
          x-struct:
          x-validate:
      required:
        - url
        - settings
        - signing_token
      title: WebhookChannel
      type: object
      x-struct:
      x-validate:
info:
  description: API for notification channel management and alert dispatch.
  title: Holter Delivery API
  version: 1.0.0
openapi: 3.0.0
paths:
  /api/v1/notification_channels/{id}:
    delete:
      callbacks: {}
      description: Permanently delete a notification channel.
      operationId: HolterWeb.Api.NotificationChannelController.delete
      parameters:
        - description: Channel UUID
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      responses:
        '204':
          content:
            application/json: {}
          description: Deleted successfully
        '404':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel not found
      summary: Delete notification channel
      tags:
        - Notification Channels
    get:
      callbacks: {}
      description: Fetch a single notification channel by its UUID.
      operationId: HolterWeb.Api.NotificationChannelController.show
      parameters:
        - description: Channel UUID
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      responses:
        '200':
          content:
            application/json:
              schema:
                description: Single notification channel response.
                properties:
                  data:
                    additionalProperties: false
                    description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
                    properties:
                      email_channel:
                        additionalProperties: false
                        description: Email-specific configuration.
                        nullable: true
                        properties:
                          address:
                            type: string
                            x-struct:
                            x-validate:
                          anti_phishing_code:
                            description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
                            type: string
                            x-struct:
                            x-validate:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                        required:
                          - address
                          - settings
                          - anti_phishing_code
                        title: EmailChannel
                        type: object
                        x-struct:
                        x-validate:
                      id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                      inserted_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      name:
                        type: string
                        x-struct:
                        x-validate:
                      type:
                        enum:
                          - email
                          - webhook
                        type: string
                        x-struct:
                        x-validate:
                      updated_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      webhook_channel:
                        additionalProperties: false
                        description: Webhook-specific configuration.
                        nullable: true
                        properties:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                          signing_token:
                            description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
                            type: string
                            x-struct:
                            x-validate:
                          url:
                            type: string
                            x-struct:
                            x-validate:
                        required:
                          - url
                          - settings
                          - signing_token
                        title: WebhookChannel
                        type: object
                        x-struct:
                        x-validate:
                      workspace_id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                    required:
                      - id
                      - workspace_id
                      - name
                      - type
                      - webhook_channel
                      - email_channel
                      - inserted_at
                      - updated_at
                    title: NotificationChannel
                    type: object
                    x-struct:
                    x-validate:
                required:
                  - data
                title: NotificationChannelResponse
                type: object
                x-struct:
                x-validate:
          description: Notification channel
        '404':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel not found
      summary: Get notification channel
      tags:
        - Notification Channels
    patch:
      callbacks: {}
      description: Update an existing notification channel.
      operationId: HolterWeb.Api.NotificationChannelController.update (2)
      parameters:
        - description: Channel UUID
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      requestBody:
        content:
          application/json:
            schema:
              additionalProperties: false
              description: Parameters for updating a notification channel.
              properties:
                name:
                  maxLength: 255
                  minLength: 1
                  type: string
                  x-struct:
                  x-validate:
                settings:
                  additionalProperties: true
                  nullable: true
                  type: object
                  x-struct:
                  x-validate:
                target:
                  maxLength: 2048
                  minLength: 1
                  type: string
                  x-struct:
                  x-validate:
              title: NotificationChannelUpdateRequest
              type: object
              x-struct:
              x-validate:
        description: Update parameters
        required: false
      responses:
        '200':
          content:
            application/json:
              schema:
                description: Single notification channel response.
                properties:
                  data:
                    additionalProperties: false
                    description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
                    properties:
                      email_channel:
                        additionalProperties: false
                        description: Email-specific configuration.
                        nullable: true
                        properties:
                          address:
                            type: string
                            x-struct:
                            x-validate:
                          anti_phishing_code:
                            description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
                            type: string
                            x-struct:
                            x-validate:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                        required:
                          - address
                          - settings
                          - anti_phishing_code
                        title: EmailChannel
                        type: object
                        x-struct:
                        x-validate:
                      id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                      inserted_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      name:
                        type: string
                        x-struct:
                        x-validate:
                      type:
                        enum:
                          - email
                          - webhook
                        type: string
                        x-struct:
                        x-validate:
                      updated_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      webhook_channel:
                        additionalProperties: false
                        description: Webhook-specific configuration.
                        nullable: true
                        properties:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                          signing_token:
                            description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
                            type: string
                            x-struct:
                            x-validate:
                          url:
                            type: string
                            x-struct:
                            x-validate:
                        required:
                          - url
                          - settings
                          - signing_token
                        title: WebhookChannel
                        type: object
                        x-struct:
                        x-validate:
                      workspace_id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                    required:
                      - id
                      - workspace_id
                      - name
                      - type
                      - webhook_channel
                      - email_channel
                      - inserted_at
                      - updated_at
                    title: NotificationChannel
                    type: object
                    x-struct:
                    x-validate:
                required:
                  - data
                title: NotificationChannelResponse
                type: object
                x-struct:
                x-validate:
          description: Updated channel
        '404':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel not found
        '422':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Validation error
      summary: Update notification channel
      tags:
        - Notification Channels
    put:
      callbacks: {}
      description: Update an existing notification channel.
      operationId: HolterWeb.Api.NotificationChannelController.update
      parameters:
        - description: Channel UUID
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      requestBody:
        content:
          application/json:
            schema:
              additionalProperties: false
              description: Parameters for updating a notification channel.
              properties:
                name:
                  maxLength: 255
                  minLength: 1
                  type: string
                  x-struct:
                  x-validate:
                settings:
                  additionalProperties: true
                  nullable: true
                  type: object
                  x-struct:
                  x-validate:
                target:
                  maxLength: 2048
                  minLength: 1
                  type: string
                  x-struct:
                  x-validate:
              title: NotificationChannelUpdateRequest
              type: object
              x-struct:
              x-validate:
        description: Update parameters
        required: false
      responses:
        '200':
          content:
            application/json:
              schema:
                description: Single notification channel response.
                properties:
                  data:
                    additionalProperties: false
                    description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
                    properties:
                      email_channel:
                        additionalProperties: false
                        description: Email-specific configuration.
                        nullable: true
                        properties:
                          address:
                            type: string
                            x-struct:
                            x-validate:
                          anti_phishing_code:
                            description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
                            type: string
                            x-struct:
                            x-validate:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                        required:
                          - address
                          - settings
                          - anti_phishing_code
                        title: EmailChannel
                        type: object
                        x-struct:
                        x-validate:
                      id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                      inserted_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      name:
                        type: string
                        x-struct:
                        x-validate:
                      type:
                        enum:
                          - email
                          - webhook
                        type: string
                        x-struct:
                        x-validate:
                      updated_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      webhook_channel:
                        additionalProperties: false
                        description: Webhook-specific configuration.
                        nullable: true
                        properties:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                          signing_token:
                            description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
                            type: string
                            x-struct:
                            x-validate:
                          url:
                            type: string
                            x-struct:
                            x-validate:
                        required:
                          - url
                          - settings
                          - signing_token
                        title: WebhookChannel
                        type: object
                        x-struct:
                        x-validate:
                      workspace_id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                    required:
                      - id
                      - workspace_id
                      - name
                      - type
                      - webhook_channel
                      - email_channel
                      - inserted_at
                      - updated_at
                    title: NotificationChannel
                    type: object
                    x-struct:
                    x-validate:
                required:
                  - data
                title: NotificationChannelResponse
                type: object
                x-struct:
                x-validate:
          description: Updated channel
        '404':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel not found
        '422':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Validation error
      summary: Update notification channel
      tags:
        - Notification Channels
  /api/v1/notification_channels/{notification_channel_id}/anti_phishing_code:
    put:
      callbacks: {}
      description: Generate a fresh anti-phishing code for an email channel. The next email through this channel will carry the new value.
      operationId: HolterWeb.Api.NotificationChannelController.rotate_anti_phishing_code
      parameters:
        - description: Channel UUID
          in: path
          name: notification_channel_id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      responses:
        '200':
          content:
            application/json:
              schema:
                description: Single notification channel response.
                properties:
                  data:
                    additionalProperties: false
                    description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
                    properties:
                      email_channel:
                        additionalProperties: false
                        description: Email-specific configuration.
                        nullable: true
                        properties:
                          address:
                            type: string
                            x-struct:
                            x-validate:
                          anti_phishing_code:
                            description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
                            type: string
                            x-struct:
                            x-validate:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                        required:
                          - address
                          - settings
                          - anti_phishing_code
                        title: EmailChannel
                        type: object
                        x-struct:
                        x-validate:
                      id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                      inserted_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      name:
                        type: string
                        x-struct:
                        x-validate:
                      type:
                        enum:
                          - email
                          - webhook
                        type: string
                        x-struct:
                        x-validate:
                      updated_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      webhook_channel:
                        additionalProperties: false
                        description: Webhook-specific configuration.
                        nullable: true
                        properties:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                          signing_token:
                            description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
                            type: string
                            x-struct:
                            x-validate:
                          url:
                            type: string
                            x-struct:
                            x-validate:
                        required:
                          - url
                          - settings
                          - signing_token
                        title: WebhookChannel
                        type: object
                        x-struct:
                        x-validate:
                      workspace_id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                    required:
                      - id
                      - workspace_id
                      - name
                      - type
                      - webhook_channel
                      - email_channel
                      - inserted_at
                      - updated_at
                    title: NotificationChannel
                    type: object
                    x-struct:
                    x-validate:
                required:
                  - data
                title: NotificationChannelResponse
                type: object
                x-struct:
                x-validate:
          description: Channel with rotated anti_phishing_code
        '404':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel not found
        '422':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel is not an email channel
      summary: Rotate the email anti-phishing code
      tags:
        - Notification Channels
  /api/v1/notification_channels/{notification_channel_id}/delivery_logs:
    get:
      callbacks: {}
      description: List delivery job logs for a notification channel with pagination and filtering.
      operationId: HolterWeb.Api.DeliveryLogController.index
      parameters:
        - description: Notification channel UUID
          in: path
          name: notification_channel_id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
        - description: Page number
          in: query
          name: page
          required: false
          schema:
            default: 1
            type: integer
            x-struct:
            x-validate:
        - description: Items per page
          in: query
          name: page_size
          required: false
          schema:
            default: 50
            type: integer
            x-struct:
            x-validate:
        - description: 'Filter by status: success or failed'
          in: query
          name: status
          required: false
          schema:
            type: string
            x-struct:
            x-validate:
        - description: 'Sort column: attempted_at or state'
          in: query
          name: sort_by
          required: false
          schema:
            type: string
            x-struct:
            x-validate:
        - description: 'Sort direction: asc or desc'
          in: query
          name: sort_dir
          required: false
          schema:
            type: string
            x-struct:
            x-validate:
      responses:
        '200':
          content:
            application/json:
              schema:
                additionalProperties: false
                description: A paginated list of delivery log entries for a notification channel.
                properties:
                  data:
                    items:
                      additionalProperties: false
                      description: A delivery job log entry for a notification channel.
                      properties:
                        attempted_at:
                          format: date-time
                          nullable: true
                          type: string
                          x-struct:
                          x-validate:
                        errors:
                          items:
                            nullable: true
                            type: string
                            x-struct:
                            x-validate:
                          type: array
                          x-struct:
                          x-validate:
                        event:
                          nullable: true
                          type: string
                          x-struct:
                          x-validate:
                        id:
                          type: integer
                          x-struct:
                          x-validate:
                        inserted_at:
                          format: date-time
                          type: string
                          x-struct:
                          x-validate:
                        status:
                          enum:
                            - success
                            - failed
                          type: string
                          x-struct:
                          x-validate:
                        worker:
                          type: string
                          x-struct:
                          x-validate:
                      required:
                        - id
                        - status
                        - worker
                        - inserted_at
                      title: DeliveryLog
                      type: object
                      x-struct:
                      x-validate:
                    type: array
                    x-struct:
                    x-validate:
                  meta:
                    additionalProperties: false
                    properties:
                      page:
                        type: integer
                        x-struct:
                        x-validate:
                      page_size:
                        type: integer
                        x-struct:
                        x-validate:
                      total_pages:
                        type: integer
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: DeliveryLogList
                type: object
                x-struct:
                x-validate:
          description: Log list
        '404':
          content:
            application/json:
              schema:
                additionalProperties: false
                description: Standard error response.
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    required:
                      - code
                      - message
                    type: object
                    x-struct:
                    x-validate:
                required:
                  - error
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel not found
      summary: List delivery logs
      tags:
        - Delivery Logs
  /api/v1/notification_channels/{notification_channel_id}/pings:
    post:
      callbacks: {}
      description: Enqueue a test notification to verify the channel is reachable.
      operationId: HolterWeb.Api.NotificationChannelController.ping
      parameters:
        - description: Channel UUID
          in: path
          name: notification_channel_id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      responses:
        '202':
          content:
            application/json: {}
          description: Ping enqueued
        '404':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel not found
      summary: Send a channel ping
      tags:
        - Notification Channels
  /api/v1/notification_channels/{notification_channel_id}/signing_token:
    put:
      callbacks: {}
      description: Generate a fresh HMAC signing token for a webhook channel. The previous token stops working at the next dispatch.
      operationId: HolterWeb.Api.NotificationChannelController.rotate_signing_token
      parameters:
        - description: Channel UUID
          in: path
          name: notification_channel_id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      responses:
        '200':
          content:
            application/json:
              schema:
                description: Single notification channel response.
                properties:
                  data:
                    additionalProperties: false
                    description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
                    properties:
                      email_channel:
                        additionalProperties: false
                        description: Email-specific configuration.
                        nullable: true
                        properties:
                          address:
                            type: string
                            x-struct:
                            x-validate:
                          anti_phishing_code:
                            description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
                            type: string
                            x-struct:
                            x-validate:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                        required:
                          - address
                          - settings
                          - anti_phishing_code
                        title: EmailChannel
                        type: object
                        x-struct:
                        x-validate:
                      id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                      inserted_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      name:
                        type: string
                        x-struct:
                        x-validate:
                      type:
                        enum:
                          - email
                          - webhook
                        type: string
                        x-struct:
                        x-validate:
                      updated_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      webhook_channel:
                        additionalProperties: false
                        description: Webhook-specific configuration.
                        nullable: true
                        properties:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                          signing_token:
                            description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
                            type: string
                            x-struct:
                            x-validate:
                          url:
                            type: string
                            x-struct:
                            x-validate:
                        required:
                          - url
                          - settings
                          - signing_token
                        title: WebhookChannel
                        type: object
                        x-struct:
                        x-validate:
                      workspace_id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                    required:
                      - id
                      - workspace_id
                      - name
                      - type
                      - webhook_channel
                      - email_channel
                      - inserted_at
                      - updated_at
                    title: NotificationChannel
                    type: object
                    x-struct:
                    x-validate:
                required:
                  - data
                title: NotificationChannelResponse
                type: object
                x-struct:
                x-validate:
          description: Channel with rotated signing_token
        '404':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel not found
        '422':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Channel is not a webhook channel
      summary: Rotate the webhook signing token
      tags:
        - Notification Channels
  /api/v1/workspaces/{workspace_slug}/notification_channels:
    get:
      callbacks: {}
      description: List all notification channels for a workspace. Optionally filter by `type`.
      operationId: HolterWeb.Api.NotificationChannelController.index
      parameters:
        - description: Workspace slug
          in: path
          name: workspace_slug
          required: true
          schema:
            type: string
            x-struct:
            x-validate:
        - description: Restrict results to channels of this type.
          in: query
          name: type
          required: false
          schema:
            enum:
              - webhook
              - email
            type: string
            x-struct:
            x-validate:
      responses:
        '200':
          content:
            application/json:
              schema:
                description: Paginated list of notification channels.
                properties:
                  data:
                    items:
                      additionalProperties: false
                      description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
                      properties:
                        email_channel:
                          additionalProperties: false
                          description: Email-specific configuration.
                          nullable: true
                          properties:
                            address:
                              type: string
                              x-struct:
                              x-validate:
                            anti_phishing_code:
                              description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
                              type: string
                              x-struct:
                              x-validate:
                            settings:
                              additionalProperties: true
                              type: object
                              x-struct:
                              x-validate:
                          required:
                            - address
                            - settings
                            - anti_phishing_code
                          title: EmailChannel
                          type: object
                          x-struct:
                          x-validate:
                        id:
                          format: uuid
                          type: string
                          x-struct:
                          x-validate:
                        inserted_at:
                          format: date-time
                          type: string
                          x-struct:
                          x-validate:
                        name:
                          type: string
                          x-struct:
                          x-validate:
                        type:
                          enum:
                            - email
                            - webhook
                          type: string
                          x-struct:
                          x-validate:
                        updated_at:
                          format: date-time
                          type: string
                          x-struct:
                          x-validate:
                        webhook_channel:
                          additionalProperties: false
                          description: Webhook-specific configuration.
                          nullable: true
                          properties:
                            settings:
                              additionalProperties: true
                              type: object
                              x-struct:
                              x-validate:
                            signing_token:
                              description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
                              type: string
                              x-struct:
                              x-validate:
                            url:
                              type: string
                              x-struct:
                              x-validate:
                          required:
                            - url
                            - settings
                            - signing_token
                          title: WebhookChannel
                          type: object
                          x-struct:
                          x-validate:
                        workspace_id:
                          format: uuid
                          type: string
                          x-struct:
                          x-validate:
                      required:
                        - id
                        - workspace_id
                        - name
                        - type
                        - webhook_channel
                        - email_channel
                        - inserted_at
                        - updated_at
                      title: NotificationChannel
                      type: object
                      x-struct:
                      x-validate:
                    type: array
                    x-struct:
                    x-validate:
                required:
                  - data
                title: NotificationChannelList
                type: object
                x-struct:
                x-validate:
          description: Notification channel list
        '404':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Workspace not found
      summary: List notification channels
      tags:
        - Notification Channels
    post:
      callbacks: {}
      description: Create a new notification channel for the specified workspace.
      operationId: HolterWeb.Api.NotificationChannelController.create
      parameters:
        - description: Workspace slug
          in: path
          name: workspace_slug
          required: true
          schema:
            type: string
            x-struct:
            x-validate:
      requestBody:
        content:
          application/json:
            schema:
              additionalProperties: false
              description: Parameters for creating a notification channel.
              properties:
                name:
                  maxLength: 255
                  minLength: 1
                  type: string
                  x-struct:
                  x-validate:
                settings:
                  additionalProperties: true
                  nullable: true
                  type: object
                  x-struct:
                  x-validate:
                target:
                  maxLength: 2048
                  minLength: 1
                  type: string
                  x-struct:
                  x-validate:
                type:
                  enum:
                    - email
                    - webhook
                  type: string
                  x-struct:
                  x-validate:
              required:
                - name
                - type
                - target
              title: NotificationChannelCreateRequest
              type: object
              x-struct:
              x-validate:
        description: Channel parameters
        required: false
      responses:
        '201':
          content:
            application/json:
              schema:
                description: Single notification channel response.
                properties:
                  data:
                    additionalProperties: false
                    description: A notification delivery channel. Carries common fields plus exactly one populated subtype object — `webhook_channel` xor `email_channel`. The other is `null`.
                    properties:
                      email_channel:
                        additionalProperties: false
                        description: Email-specific configuration.
                        nullable: true
                        properties:
                          address:
                            type: string
                            x-struct:
                            x-validate:
                          anti_phishing_code:
                            description: Visual anti-phishing code printed in every email. Rotate via PUT /api/v1/notification_channels/:id/anti_phishing_code.
                            type: string
                            x-struct:
                            x-validate:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                        required:
                          - address
                          - settings
                          - anti_phishing_code
                        title: EmailChannel
                        type: object
                        x-struct:
                        x-validate:
                      id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                      inserted_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      name:
                        type: string
                        x-struct:
                        x-validate:
                      type:
                        enum:
                          - email
                          - webhook
                        type: string
                        x-struct:
                        x-validate:
                      updated_at:
                        format: date-time
                        type: string
                        x-struct:
                        x-validate:
                      webhook_channel:
                        additionalProperties: false
                        description: Webhook-specific configuration.
                        nullable: true
                        properties:
                          settings:
                            additionalProperties: true
                            type: object
                            x-struct:
                            x-validate:
                          signing_token:
                            description: HMAC-SHA256 signing key. Sensitive — keep private. Rotate via PUT /api/v1/notification_channels/:id/signing_token.
                            type: string
                            x-struct:
                            x-validate:
                          url:
                            type: string
                            x-struct:
                            x-validate:
                        required:
                          - url
                          - settings
                          - signing_token
                        title: WebhookChannel
                        type: object
                        x-struct:
                        x-validate:
                      workspace_id:
                        format: uuid
                        type: string
                        x-struct:
                        x-validate:
                    required:
                      - id
                      - workspace_id
                      - name
                      - type
                      - webhook_channel
                      - email_channel
                      - inserted_at
                      - updated_at
                    title: NotificationChannel
                    type: object
                    x-struct:
                    x-validate:
                required:
                  - data
                title: NotificationChannelResponse
                type: object
                x-struct:
                x-validate:
          description: Created channel
        '422':
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        type: string
                        x-struct:
                        x-validate:
                      message:
                        type: string
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Validation error
      summary: Create notification channel
      tags:
        - Notification Channels
security: []
servers:
  - url: /
    variables: {}
tags: []
