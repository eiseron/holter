---
components:
  responses: {}
  schemas:
    Error:
      description: Standard error response.
      properties:
        errors:
          additionalProperties:
            items:
              type: string
              x-struct:
              x-validate:
            type: array
            x-struct:
            x-validate:
          type: object
          x-struct:
          x-validate:
      title: Error
      type: object
      x-struct:
      x-validate:
    Monitor:
      description: A monitoring target for HTTP/HTTPS/SSL checks.
      properties:
        health_status:
          enum:
            - up
            - down
            - degraded
            - compromised
            - unknown
          type: string
          x-struct:
          x-validate:
        id:
          format: uuid
          type: string
          x-struct:
          x-validate:
        inserted_at:
          format: date-time
          type: string
          x-struct:
          x-validate:
        interval_seconds:
          maximum: 86400
          minimum: 1
          type: integer
          x-struct:
          x-validate:
        last_checked_at:
          format: date-time
          nullable: true
          type: string
          x-struct:
          x-validate:
        logical_state:
          enum:
            - active
            - paused
            - archived
          type: string
          x-struct:
          x-validate:
        method:
          enum:
            - get
            - post
            - put
            - patch
            - delete
            - options
            - head
          type: string
          x-struct:
          x-validate:
        timeout_seconds:
          maximum: 300
          minimum: 1
          type: integer
          x-struct:
          x-validate:
        updated_at:
          format: date-time
          type: string
          x-struct:
          x-validate:
        url:
          format: uri
          type: string
          x-struct:
          x-validate:
      required:
        - id
        - url
        - method
        - interval_seconds
        - timeout_seconds
      title: Monitor
      type: object
      x-struct:
      x-validate:
    MonitorList:
      description: A list of monitors with metadata.
      properties:
        data:
          items:
            description: A monitoring target for HTTP/HTTPS/SSL checks.
            properties:
              health_status:
                enum:
                  - up
                  - down
                  - degraded
                  - compromised
                  - unknown
                type: string
                x-struct:
                x-validate:
              id:
                format: uuid
                type: string
                x-struct:
                x-validate:
              inserted_at:
                format: date-time
                type: string
                x-struct:
                x-validate:
              interval_seconds:
                maximum: 86400
                minimum: 1
                type: integer
                x-struct:
                x-validate:
              last_checked_at:
                format: date-time
                nullable: true
                type: string
                x-struct:
                x-validate:
              logical_state:
                enum:
                  - active
                  - paused
                  - archived
                type: string
                x-struct:
                x-validate:
              method:
                enum:
                  - get
                  - post
                  - put
                  - patch
                  - delete
                  - options
                  - head
                type: string
                x-struct:
                x-validate:
              timeout_seconds:
                maximum: 300
                minimum: 1
                type: integer
                x-struct:
                x-validate:
              updated_at:
                format: date-time
                type: string
                x-struct:
                x-validate:
              url:
                format: uri
                type: string
                x-struct:
                x-validate:
            required:
              - id
              - url
              - method
              - interval_seconds
              - timeout_seconds
            title: Monitor
            type: object
            x-struct:
            x-validate:
          type: array
          x-struct:
          x-validate:
        meta:
          properties:
            page:
              type: integer
              x-struct:
              x-validate:
            page_size:
              type: integer
              x-struct:
              x-validate:
            total:
              type: integer
              x-struct:
              x-validate:
          type: object
          x-struct:
          x-validate:
      title: MonitorList
      type: object
      x-struct:
      x-validate:
    MonitorRequest:
      description: Parameters for creating or updating a monitor.
      properties:
        interval_seconds:
          maximum: 86400
          minimum: 1
          type: integer
          x-struct:
          x-validate:
        method:
          enum:
            - get
            - post
            - put
            - patch
            - delete
            - options
            - head
          type: string
          x-struct:
          x-validate:
        raw_headers:
          nullable: true
          type: string
          x-struct:
          x-validate:
        raw_keyword_negative:
          nullable: true
          type: string
          x-struct:
          x-validate:
        raw_keyword_positive:
          nullable: true
          type: string
          x-struct:
          x-validate:
        ssl_ignore:
          default: false
          type: boolean
          x-struct:
          x-validate:
        timeout_seconds:
          maximum: 300
          minimum: 1
          type: integer
          x-struct:
          x-validate:
        url:
          format: uri
          type: string
          x-struct:
          x-validate:
      required:
        - url
        - method
        - interval_seconds
      title: MonitorRequest
      type: object
      x-struct:
      x-validate:
info:
  description: API for monitoring and security scanning.
  title: Holter API
  version: 1.0.0
openapi: 3.0.0
paths:
  /api/orgs/{org_slug}/monitoring/monitors:
    get:
      callbacks: {}
      description: List all monitors for an organization with pagination and filtering.
      operationId: HolterWeb.MonitorController.index
      parameters:
        - description: Organization slug
          example: eiseron
          in: path
          name: org_slug
          required: true
          schema:
            type: string
            x-struct:
            x-validate:
        - description: Page number
          in: query
          name: page
          required: false
          schema:
            default: 1
            type: integer
            x-struct:
            x-validate:
        - description: Items per page
          in: query
          name: page_size
          required: false
          schema:
            default: 25
            type: integer
            x-struct:
            x-validate:
        - description: Filter by health status
          in: query
          name: health_status
          required: false
          schema:
            type: string
            x-struct:
            x-validate:
        - description: Filter by logical state
          in: query
          name: logical_state
          required: false
          schema:
            type: string
            x-struct:
            x-validate:
      responses:
        '200':
          content:
            application/json:
              schema:
                description: A list of monitors with metadata.
                properties:
                  data:
                    items:
                      description: A monitoring target for HTTP/HTTPS/SSL checks.
                      properties:
                        health_status:
                          enum:
                            - up
                            - down
                            - degraded
                            - compromised
                            - unknown
                          type: string
                          x-struct:
                          x-validate:
                        id:
                          format: uuid
                          type: string
                          x-struct:
                          x-validate:
                        inserted_at:
                          format: date-time
                          type: string
                          x-struct:
                          x-validate:
                        interval_seconds:
                          maximum: 86400
                          minimum: 1
                          type: integer
                          x-struct:
                          x-validate:
                        last_checked_at:
                          format: date-time
                          nullable: true
                          type: string
                          x-struct:
                          x-validate:
                        logical_state:
                          enum:
                            - active
                            - paused
                            - archived
                          type: string
                          x-struct:
                          x-validate:
                        method:
                          enum:
                            - get
                            - post
                            - put
                            - patch
                            - delete
                            - options
                            - head
                          type: string
                          x-struct:
                          x-validate:
                        timeout_seconds:
                          maximum: 300
                          minimum: 1
                          type: integer
                          x-struct:
                          x-validate:
                        updated_at:
                          format: date-time
                          type: string
                          x-struct:
                          x-validate:
                        url:
                          format: uri
                          type: string
                          x-struct:
                          x-validate:
                      required:
                        - id
                        - url
                        - method
                        - interval_seconds
                        - timeout_seconds
                      title: Monitor
                      type: object
                      x-struct:
                      x-validate:
                    type: array
                    x-struct:
                    x-validate:
                  meta:
                    properties:
                      page:
                        type: integer
                        x-struct:
                        x-validate:
                      page_size:
                        type: integer
                        x-struct:
                        x-validate:
                      total:
                        type: integer
                        x-struct:
                        x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: MonitorList
                type: object
                x-struct:
                x-validate:
          description: Monitor list
        '404':
          content:
            application/json:
              schema:
                description: Standard error response.
                properties:
                  errors:
                    additionalProperties:
                      items:
                        type: string
                        x-struct:
                        x-validate:
                      type: array
                      x-struct:
                      x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Organization not found
      summary: List monitors
      tags:
        - Monitors
    post:
      callbacks: {}
      description: Create a new monitor for the specified organization.
      operationId: HolterWeb.MonitorController.create
      parameters:
        - description: Organization slug
          in: path
          name: org_slug
          required: true
          schema:
            type: string
            x-struct:
            x-validate:
      requestBody:
        content:
          application/json:
            schema:
              description: Parameters for creating or updating a monitor.
              properties:
                interval_seconds:
                  maximum: 86400
                  minimum: 1
                  type: integer
                  x-struct:
                  x-validate:
                method:
                  enum:
                    - get
                    - post
                    - put
                    - patch
                    - delete
                    - options
                    - head
                  type: string
                  x-struct:
                  x-validate:
                raw_headers:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                raw_keyword_negative:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                raw_keyword_positive:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                ssl_ignore:
                  default: false
                  type: boolean
                  x-struct:
                  x-validate:
                timeout_seconds:
                  maximum: 300
                  minimum: 1
                  type: integer
                  x-struct:
                  x-validate:
                url:
                  format: uri
                  type: string
                  x-struct:
                  x-validate:
              required:
                - url
                - method
                - interval_seconds
              title: MonitorRequest
              type: object
              x-struct:
              x-validate:
        description: Monitor parameters
        required: false
      responses:
        '201':
          content:
            application/json:
              schema:
                description: A monitoring target for HTTP/HTTPS/SSL checks.
                properties:
                  health_status:
                    enum:
                      - up
                      - down
                      - degraded
                      - compromised
                      - unknown
                    type: string
                    x-struct:
                    x-validate:
                  id:
                    format: uuid
                    type: string
                    x-struct:
                    x-validate:
                  inserted_at:
                    format: date-time
                    type: string
                    x-struct:
                    x-validate:
                  interval_seconds:
                    maximum: 86400
                    minimum: 1
                    type: integer
                    x-struct:
                    x-validate:
                  last_checked_at:
                    format: date-time
                    nullable: true
                    type: string
                    x-struct:
                    x-validate:
                  logical_state:
                    enum:
                      - active
                      - paused
                      - archived
                    type: string
                    x-struct:
                    x-validate:
                  method:
                    enum:
                      - get
                      - post
                      - put
                      - patch
                      - delete
                      - options
                      - head
                    type: string
                    x-struct:
                    x-validate:
                  timeout_seconds:
                    maximum: 300
                    minimum: 1
                    type: integer
                    x-struct:
                    x-validate:
                  updated_at:
                    format: date-time
                    type: string
                    x-struct:
                    x-validate:
                  url:
                    format: uri
                    type: string
                    x-struct:
                    x-validate:
                required:
                  - id
                  - url
                  - method
                  - interval_seconds
                  - timeout_seconds
                title: Monitor
                type: object
                x-struct:
                x-validate:
          description: Created monitor
        '422':
          content:
            application/json:
              schema:
                description: Standard error response.
                properties:
                  errors:
                    additionalProperties:
                      items:
                        type: string
                        x-struct:
                        x-validate:
                      type: array
                      x-struct:
                      x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Validation error
      summary: Create monitor
      tags:
        - Monitors
  /api/orgs/{org_slug}/monitoring/monitors/{id}:
    delete:
      callbacks: {}
      description: Permanently delete a monitor and all its associated data.
      operationId: HolterWeb.MonitorController.delete
      parameters:
        - description: Organization slug
          in: path
          name: org_slug
          required: true
          schema:
            type: string
            x-struct:
            x-validate:
        - description: Monitor UUID
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      responses:
        '204':
          content:
            application/json: {}
          description: Deleted successfully
        '404':
          content:
            application/json:
              schema:
                description: Standard error response.
                properties:
                  errors:
                    additionalProperties:
                      items:
                        type: string
                        x-struct:
                        x-validate:
                      type: array
                      x-struct:
                      x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Monitor not found
      summary: Delete monitor
      tags:
        - Monitors
    get:
      callbacks: {}
      description: Fetch a single monitor by its UUID.
      operationId: HolterWeb.MonitorController.show
      parameters:
        - description: Organization slug
          in: path
          name: org_slug
          required: true
          schema:
            type: string
            x-struct:
            x-validate:
        - description: Monitor UUID
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      responses:
        '200':
          content:
            application/json:
              schema:
                description: A monitoring target for HTTP/HTTPS/SSL checks.
                properties:
                  health_status:
                    enum:
                      - up
                      - down
                      - degraded
                      - compromised
                      - unknown
                    type: string
                    x-struct:
                    x-validate:
                  id:
                    format: uuid
                    type: string
                    x-struct:
                    x-validate:
                  inserted_at:
                    format: date-time
                    type: string
                    x-struct:
                    x-validate:
                  interval_seconds:
                    maximum: 86400
                    minimum: 1
                    type: integer
                    x-struct:
                    x-validate:
                  last_checked_at:
                    format: date-time
                    nullable: true
                    type: string
                    x-struct:
                    x-validate:
                  logical_state:
                    enum:
                      - active
                      - paused
                      - archived
                    type: string
                    x-struct:
                    x-validate:
                  method:
                    enum:
                      - get
                      - post
                      - put
                      - patch
                      - delete
                      - options
                      - head
                    type: string
                    x-struct:
                    x-validate:
                  timeout_seconds:
                    maximum: 300
                    minimum: 1
                    type: integer
                    x-struct:
                    x-validate:
                  updated_at:
                    format: date-time
                    type: string
                    x-struct:
                    x-validate:
                  url:
                    format: uri
                    type: string
                    x-struct:
                    x-validate:
                required:
                  - id
                  - url
                  - method
                  - interval_seconds
                  - timeout_seconds
                title: Monitor
                type: object
                x-struct:
                x-validate:
          description: Monitor details
        '404':
          content:
            application/json:
              schema:
                description: Standard error response.
                properties:
                  errors:
                    additionalProperties:
                      items:
                        type: string
                        x-struct:
                        x-validate:
                      type: array
                      x-struct:
                      x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Monitor not found
      summary: Get monitor
      tags:
        - Monitors
    patch:
      callbacks: {}
      description: Update an existing monitor's configuration.
      operationId: HolterWeb.MonitorController.update (2)
      parameters:
        - description: Organization slug
          in: path
          name: org_slug
          required: true
          schema:
            type: string
            x-struct:
            x-validate:
        - description: Monitor UUID
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      requestBody:
        content:
          application/json:
            schema:
              description: Parameters for creating or updating a monitor.
              properties:
                interval_seconds:
                  maximum: 86400
                  minimum: 1
                  type: integer
                  x-struct:
                  x-validate:
                method:
                  enum:
                    - get
                    - post
                    - put
                    - patch
                    - delete
                    - options
                    - head
                  type: string
                  x-struct:
                  x-validate:
                raw_headers:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                raw_keyword_negative:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                raw_keyword_positive:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                ssl_ignore:
                  default: false
                  type: boolean
                  x-struct:
                  x-validate:
                timeout_seconds:
                  maximum: 300
                  minimum: 1
                  type: integer
                  x-struct:
                  x-validate:
                url:
                  format: uri
                  type: string
                  x-struct:
                  x-validate:
              required:
                - url
                - method
                - interval_seconds
              title: MonitorRequest
              type: object
              x-struct:
              x-validate:
        description: Update parameters
        required: false
      responses:
        '200':
          content:
            application/json:
              schema:
                description: A monitoring target for HTTP/HTTPS/SSL checks.
                properties:
                  health_status:
                    enum:
                      - up
                      - down
                      - degraded
                      - compromised
                      - unknown
                    type: string
                    x-struct:
                    x-validate:
                  id:
                    format: uuid
                    type: string
                    x-struct:
                    x-validate:
                  inserted_at:
                    format: date-time
                    type: string
                    x-struct:
                    x-validate:
                  interval_seconds:
                    maximum: 86400
                    minimum: 1
                    type: integer
                    x-struct:
                    x-validate:
                  last_checked_at:
                    format: date-time
                    nullable: true
                    type: string
                    x-struct:
                    x-validate:
                  logical_state:
                    enum:
                      - active
                      - paused
                      - archived
                    type: string
                    x-struct:
                    x-validate:
                  method:
                    enum:
                      - get
                      - post
                      - put
                      - patch
                      - delete
                      - options
                      - head
                    type: string
                    x-struct:
                    x-validate:
                  timeout_seconds:
                    maximum: 300
                    minimum: 1
                    type: integer
                    x-struct:
                    x-validate:
                  updated_at:
                    format: date-time
                    type: string
                    x-struct:
                    x-validate:
                  url:
                    format: uri
                    type: string
                    x-struct:
                    x-validate:
                required:
                  - id
                  - url
                  - method
                  - interval_seconds
                  - timeout_seconds
                title: Monitor
                type: object
                x-struct:
                x-validate:
          description: Updated monitor
        '404':
          content:
            application/json:
              schema:
                description: Standard error response.
                properties:
                  errors:
                    additionalProperties:
                      items:
                        type: string
                        x-struct:
                        x-validate:
                      type: array
                      x-struct:
                      x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Monitor not found
        '422':
          content:
            application/json:
              schema:
                description: Standard error response.
                properties:
                  errors:
                    additionalProperties:
                      items:
                        type: string
                        x-struct:
                        x-validate:
                      type: array
                      x-struct:
                      x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Validation error
      summary: Update monitor
      tags:
        - Monitors
    put:
      callbacks: {}
      description: Update an existing monitor's configuration.
      operationId: HolterWeb.MonitorController.update
      parameters:
        - description: Organization slug
          in: path
          name: org_slug
          required: true
          schema:
            type: string
            x-struct:
            x-validate:
        - description: Monitor UUID
          in: path
          name: id
          required: true
          schema:
            format: uuid
            type: string
            x-struct:
            x-validate:
      requestBody:
        content:
          application/json:
            schema:
              description: Parameters for creating or updating a monitor.
              properties:
                interval_seconds:
                  maximum: 86400
                  minimum: 1
                  type: integer
                  x-struct:
                  x-validate:
                method:
                  enum:
                    - get
                    - post
                    - put
                    - patch
                    - delete
                    - options
                    - head
                  type: string
                  x-struct:
                  x-validate:
                raw_headers:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                raw_keyword_negative:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                raw_keyword_positive:
                  nullable: true
                  type: string
                  x-struct:
                  x-validate:
                ssl_ignore:
                  default: false
                  type: boolean
                  x-struct:
                  x-validate:
                timeout_seconds:
                  maximum: 300
                  minimum: 1
                  type: integer
                  x-struct:
                  x-validate:
                url:
                  format: uri
                  type: string
                  x-struct:
                  x-validate:
              required:
                - url
                - method
                - interval_seconds
              title: MonitorRequest
              type: object
              x-struct:
              x-validate:
        description: Update parameters
        required: false
      responses:
        '200':
          content:
            application/json:
              schema:
                description: A monitoring target for HTTP/HTTPS/SSL checks.
                properties:
                  health_status:
                    enum:
                      - up
                      - down
                      - degraded
                      - compromised
                      - unknown
                    type: string
                    x-struct:
                    x-validate:
                  id:
                    format: uuid
                    type: string
                    x-struct:
                    x-validate:
                  inserted_at:
                    format: date-time
                    type: string
                    x-struct:
                    x-validate:
                  interval_seconds:
                    maximum: 86400
                    minimum: 1
                    type: integer
                    x-struct:
                    x-validate:
                  last_checked_at:
                    format: date-time
                    nullable: true
                    type: string
                    x-struct:
                    x-validate:
                  logical_state:
                    enum:
                      - active
                      - paused
                      - archived
                    type: string
                    x-struct:
                    x-validate:
                  method:
                    enum:
                      - get
                      - post
                      - put
                      - patch
                      - delete
                      - options
                      - head
                    type: string
                    x-struct:
                    x-validate:
                  timeout_seconds:
                    maximum: 300
                    minimum: 1
                    type: integer
                    x-struct:
                    x-validate:
                  updated_at:
                    format: date-time
                    type: string
                    x-struct:
                    x-validate:
                  url:
                    format: uri
                    type: string
                    x-struct:
                    x-validate:
                required:
                  - id
                  - url
                  - method
                  - interval_seconds
                  - timeout_seconds
                title: Monitor
                type: object
                x-struct:
                x-validate:
          description: Updated monitor
        '404':
          content:
            application/json:
              schema:
                description: Standard error response.
                properties:
                  errors:
                    additionalProperties:
                      items:
                        type: string
                        x-struct:
                        x-validate:
                      type: array
                      x-struct:
                      x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Monitor not found
        '422':
          content:
            application/json:
              schema:
                description: Standard error response.
                properties:
                  errors:
                    additionalProperties:
                      items:
                        type: string
                        x-struct:
                        x-validate:
                      type: array
                      x-struct:
                      x-validate:
                    type: object
                    x-struct:
                    x-validate:
                title: Error
                type: object
                x-struct:
                x-validate:
          description: Validation error
      summary: Update monitor
      tags:
        - Monitors
security: []
servers:
  - url: http://localhost:4000/api
    variables: {}
tags: []
